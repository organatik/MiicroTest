import { SelectAccountTypePipe } from './select-account-type.pipe';

describe('SelectAccountTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectAccountTypePipe();
    expect(pipe).toBeTruthy();
  });
});
