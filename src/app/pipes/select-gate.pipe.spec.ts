import { SelectGatePipe } from './select-gate.pipe';

describe('SelectGatePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectGatePipe();
    expect(pipe).toBeTruthy();
  });
});
