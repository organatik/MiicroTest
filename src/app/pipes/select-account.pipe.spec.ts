import { SelectAccountPipe } from './select-account.pipe';

describe('SelectAccountPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectAccountPipe();
    expect(pipe).toBeTruthy();
  });
});
